# Replace the double space from problem 3 with single spaces.

a = "John is good  boy"

print(a.replace("  "," "))
print(a) # strinngs are immutable which means that you cannot change by running functions on them